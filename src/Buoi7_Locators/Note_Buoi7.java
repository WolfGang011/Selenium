package Buoi7_Locators;

public class Note_Buoi7 {

    //  Xác định Locator những cách sau

    // - Thuộc tính : ID, name, classname, linktext, partialLinkText (chỉ lấy  1 phần của linktext)
    // - Css selectors : color, background, background-image, type của input, placeholder,...
    // Xpath : tương đối(//), tuyệt đối (/)


}
